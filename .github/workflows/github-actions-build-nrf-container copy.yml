name: Build and publish nrf connectsdk docker image to ghcr.io

on:
  workflow_dispatch:

jobs:
  docker_publish:
    runs-on: "ubuntu-20.04"

    steps:
      - uses: actions/checkout@v4
      - name: Build and publish a Docker image for ${{ github.repository }}
        uses: macbre/push-to-ghcr@master
        with:
          image_name: ${{ github.repository }}-nrf_connectsdk
          github_token: ${{ secrets.GHCR_TOKEN }}
          dockerfile: "${{ github.workspace }}/tools/nrf_connectsdk/docker/Dockerfile"
          context: "${{ github.workspace }}/tools/nrf_connectsdk/docker"